{
  "extends": "./tsconfig.json",
  "_comment": "Compiles the app AND src/scripts/seed.ts (-> dist/scripts/seed.js) so the container can seed without ts-node. e2e/specs are excluded so the entrypoint stays dist/main.js.",
  "exclude": ["node_modules", "dist", "e2e", "**/*spec.ts", "playwright.config.ts"]
}
